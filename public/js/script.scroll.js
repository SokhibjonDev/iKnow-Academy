window.addEventListener(
    "scroll",
    function () {
        if (window.scrollY > 80) {
            document.querySelector("nav").style.background = "#fff";
        } else {
            document.querySelector("nav").style.background = "#fff";
        }
    }
);